import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const commandLineImporterSelectors = importerSelectors(
  'commandLine.importer',
);

export default commandLineImporterSelectors;
